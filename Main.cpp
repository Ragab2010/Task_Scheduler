#include "TaskScheduler.h"

int main() {
    TaskScheduler taskScheduler; // Create an instance of the TaskScheduler class
    taskScheduler.Run(); // Start the task scheduler

    return 0; // Return 0 to indicate successful execution
}